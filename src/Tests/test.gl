extern int printf(char *str, ...);
extern void free(void *ptr);
extern char *malloc(int:64 size);

delegate int printf_t(char *str, ...);

char global1[20] = "adsss";

typedef struct TT_tag {
	int i;
	int p;
	struct TT_tag *o;
} TT;

typedef struct ChainTable_tag {
	int Data;
	struct ChainTable_tag *next;
} ChainTable;

int main()
{
	ChainTable header;
	ChainTable *next;

	//header.Data = 10;
	//next = malloc(sizeof(ChainTable));
	//next->Data = 110;
	//header.next = next;
	//printf("%d\n", header.next->Data);
	


	return 0;
}
